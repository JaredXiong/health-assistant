<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healthy.mapper.HealthDataMapper">

    <resultMap id="HealthDataResultMap" type="com.healthy.entity.HealthData">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="heartRate" column="heart_rate"/>
        <result property="systolicBp" column="systolic_bp"/>
        <result property="diastolicBp" column="diastolic_bp"/>
        <result property="bloodOxygen" column="blood_oxygen"/>
        <result property="bloodSugar" column="blood_sugar"/>
        <result property="bodyTemperature" column="body_temperature"/>
        <result property="respiratoryRate" column="respiratory_rate"/>
        <result property="measurementTime" column="measurement_time"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <insert id="insert" parameterType="com.healthy.entity.HealthData"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO health_data (
            user_id, heart_rate, systolic_bp, diastolic_bp,
            blood_oxygen, blood_sugar, body_temperature,
            respiratory_rate, measurement_time
        ) VALUES (
                     #{userId}, #{heartRate}, #{systolicBp}, #{diastolicBp},
                     #{bloodOxygen}, #{bloodSugar}, #{bodyTemperature},
                     #{respiratoryRate}, #{measurementTime}
                 )
    </insert>

    <select id="selectById" resultMap="HealthDataResultMap">
        SELECT * FROM health_data WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="HealthDataResultMap">
        SELECT * FROM health_data
        WHERE user_id = #{userId}
        <if test="startTime != null">
            AND measurement_time <![CDATA[ >= ]]> #{startTime}
        </if>
        <if test="endTime != null">
            AND measurement_time <![CDATA[ <= ]]> #{endTime}
        </if>
        ORDER BY measurement_time DESC
    </select>

    <select id="selectLatestByUserId" resultMap="HealthDataResultMap">
        SELECT * FROM health_data
        WHERE user_id = #{userId}
        ORDER BY measurement_time DESC
            LIMIT 1
    </select>

    <!-- 分页查询 -->
    <select id="selectByUserIdWithPage" resultMap="HealthDataResultMap">
        SELECT * FROM health_data
        WHERE user_id = #{userId}
        <if test="startTime != null">
            AND measurement_time <![CDATA[ >= ]]> #{startTime}
        </if>
        <if test="endTime != null">
            AND measurement_time <![CDATA[ <= ]]> #{endTime}
        </if>
        <if test="excludeId != null">
            AND id != #{excludeId}
        </if>
        ORDER BY measurement_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 统计总数 -->
    <select id="countByUserId" resultType="long">
        SELECT COUNT(*) FROM health_data
        WHERE user_id = #{userId}
        <if test="startTime != null">
            AND measurement_time <![CDATA[ >= ]]> #{startTime}
        </if>
        <if test="endTime != null">
            AND measurement_time <![CDATA[ <= ]]> #{endTime}
        </if>
        <if test="excludeId != null">
            AND id != #{excludeId}
        </if>
    </select>

    <select id="selectByUserIdWithRange" resultMap="HealthDataResultMap">
        SELECT * FROM health_data
        WHERE user_id = #{userId}
          AND measurement_time BETWEEN #{startTime} AND #{endTime}
        ORDER BY measurement_time ASC
    </select>
</mapper>