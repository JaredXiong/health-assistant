<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healthy.mapper.PrescriptionRecordMapper">

    <!-- 按月份统计就诊记录数量 -->
    <select id="getMonthlyCount" resultType="java.util.Map">
        SELECT DATE_FORMAT(visit_date, '%Y-%m') as month,
               COUNT(*) as count
        FROM prescription_records
        WHERE user_id = #{userId}
        GROUP BY month
        ORDER BY month DESC
    </select>

</mapper>